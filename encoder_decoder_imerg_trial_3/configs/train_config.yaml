experiment:
  name: "vae_3d_experiment"
  output_dir: "./outputs"


data:
  input_shape: [1, 12, 360, 516]

  batch_size: 8
  num_workers: 24
  pin_memory: true
  persistent_workers: true

model:
  latent_dim: 32
  checkpoint_path: "./checkpoints/vae_3d.pth"

training:
  epochs: 200
  learning_rate: 0.0001
  beta: 1.5
  device: "cuda"
  use_amp: false
  grad_clip: 1.0
  checkpoint_interval: 2

val_loader:
